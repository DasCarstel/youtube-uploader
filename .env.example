# YouTube Gaming Video Uploader - Konfiguration
# =====================================================

# Pfad zu den Aufnahmen (erforderlich)
# Beispiel für WSL mit gemountetem N: Laufwerk:
RECORDINGS_PATH=/home/carst/n_drive/AUFNAHMEN

# Standard-Sichtbarkeit für hochgeladene Videos
# Optionen: private, unlisted, public
DEFAULT_VISIBILITY=unlisted

# Debug-Modus für detaillierte Ausgaben
# Optionen: true, false
DEBUG_MODE=false

# YouTube Data API v3 Credentials
# Diese werden automatisch über OAuth2 verwaltet
# Sie benötigen eine credentials.json Datei von der Google Cloud Console
